import * as chai from 'chai';

const assert = chai.assert;

describe('Chai', () => {
  it('it can test equal', () => {
    assert(1 === 1);
  });
});

describe('Promise is a Class', () => {
  it('it has "then" function', () => {});
});
